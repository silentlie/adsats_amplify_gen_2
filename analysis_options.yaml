include: package:flutter_lints/flutter.yaml
analyzer:
  exclude:
    - lib/models/**
    - lib/*.g.dart
  plugins:
    - custom_lint

# custom_lint:
#   rules:
#     - riverpod_provider_dependencies: false